name: .NET Build # 2
run-name: Build #${{ github.run_number }} # This sets the clean name on the Actions page

env:
  FORCE_JAVASCRIPT_ACTIONS_TO_NODE24: true

on:
  push:
    paths:
      - '**.yml'

jobs:
  build:
    runs-on: windows-latest
    permissions:
      contents: write # This grants permission to create and overwrite the Release asset

    steps:
    - name: Checkout Source Code
      uses: actions/checkout@v4

    - name: Setup .NET SDK
      uses: actions/setup-dotnet@v4
      with:
        dotnet-version: '10.0.x' 

    - name: Build Project (Release)
      working-directory: ./code
      run: |
        dotnet build -c Release
        
    - name: Prepare metadata 
      working-directory: ./.github/workflows
      run: |
        dotnet build metadata.targets --target:Metadata

    - name: Package Application Binaries (Native PowerShell)
      shell: powershell
      run: |
        Compress-Archive -Path ./code/output.AnyCPU.Release/* -DestinationPath ${{ ReleaseFileName }}

    - name: Publish to GitHub Releases
      uses: softprops/action-gh-release@v2
      with:
        tag_name: ${{ ReleaseTagName }}
        name: ${{ ReleaseName }}
        body: ${{ ReleaseBody }}
        files: ${{ ReleaseFileName }}
